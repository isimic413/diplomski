using System;

namespace ExamPreparation.DAL
{
    class Start
    {
        [STAThread]
        static void Main(string[] args)
        {
            //  Application.Run(new shell()); 
        }
    }
}
